sudo: enabled

language: python

python:
  - "3.6.8"

install:
  - pip install requests
  - pip install -r requirements.txt

before_script:
  - python manage.py runserver &

script:
  - python manage.py runserver
